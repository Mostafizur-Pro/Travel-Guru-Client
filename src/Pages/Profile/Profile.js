import React from "react";
import useTitle from "./../../Hooks/useTitle";

const Profile = () => {
  useTitle("Profile");
  return (
    <div>
      <h3>Hi</h3>
    </div>
  );
};

export default Profile;
